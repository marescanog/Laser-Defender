using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PMBossMetCol : MonoBehaviour
{
    private void OnCollisionEnter2D(Collision2D collision)
    {
        /*
        NewMeteorScript isMeteor = collision.gameObject.GetComponent<NewMeteorScript>();

        if (isMeteor == null)
        {
            // do nothing
        }
        else
        {
            isMeteor.Flip_velocity();
        }
        */
    }
}
